# Franky Configuration - MINIMAL (API key only)
#
# Absolute minimum configuration - just OpenAI API key
# Everything else will use defaults or be disabled
#
# Usage: cp .env.example.minimal .env

# ============================================================
# REQUIRED: OpenAI API
# ============================================================
OPENAI_API_KEY=sk-proj-YOUR_API_KEY_HERE

# That's it! All other settings are optional.
# The bot will work with just this API key.
